// import { ROUTES } from './routeDefinitions';

// export const getOrganizationDetailsUrl = (publicOrganizationId: string): string =>
//     `${ROUTES.organization.organizationDetails.split('/:')[0]}/${publicOrganizationId}`;
